#include <gtest/gtest.h>
#include "../include/Vec_funkcijos.h" // Adjust to your header file structure
/*
TEST(ZmogusTest, DefaultConstructor) {
    Zmogus z;
    EXPECT_EQ(z.getVardas(), "");
    EXPECT_EQ(z.getPavarde(), "");
}

TEST(ZmogusTest, ParameterizedConstructor) {
    Zmogus z("Jonas", "Jonaitis");
    EXPECT_EQ(z.getVardas(), "Jonas");
    EXPECT_EQ(z.getPavarde(), "Jonaitis");
}
*/